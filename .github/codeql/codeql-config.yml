name: "email-text-tools CodeQL config"

# Exclude code that is not part of the published runtime from analysis.
# These paths generate only false-positive noise:
#   - tests and fixtures intentionally feed malformed/hostile input
#   - benchmarks and examples are developer-only helper scripts
paths-ignore:
    - test
    - '**/test/**'
    - benchmark
    - examples
